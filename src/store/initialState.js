const initialState = {
    session: {
        tradeApi: {
            accessToken: '',
        },
        coinRay: {
            accessToken: '',
        }
    },
    settings: {
        languageCode: 'en',
        themeMode: 'light',
    }
};

export default initialState;
