const initialState = {
    session: {
        tradeApi: {
            accessToken: '',
        },
        coinRay: {
            accessToken: '',
        }
    },
    settings: {
        languageCode: 'en',
        darkStyle: false,
    }
};

export default initialState;
